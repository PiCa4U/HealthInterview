import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    gap: 8,
  },
  footer: {
    flexDirection: 'row',
    gap: 16,
  },
  btn: {
    paddingHorizontal: 12,
    paddingVertical: 4,
  },
});
